{
  "metadata": {
    "version": "1.0",
    "language": "typescript"
  },
  
  "contracts": [
    {
      "id": "order_creation",
      "name": "Order Creation Flow",
      "enabled": true,
      "description": "Monitor order creation endpoint for proper observability",
      "routes": [
        "POST /api/orders"
      ],
      "code_flow": [
        {
          "layer": "controller",
          "file_pattern": "orders"
        },
        {
          "layer": "service",
          "file_pattern": "order-service"
        },
        {
          "layer": "database",
          "file_pattern": "repository"
        }
      ]
    },
    
    {
      "id": "order_fulfillment",
      "name": "Order Fulfillment Flow",
      "enabled": true,
      "description": "Monitor order fulfillment for inventory and delivery tracking",
      "routes": [
        "POST /api/orders/:id/fulfill"
      ],
      "code_flow": [
        {
          "layer": "controller",
          "file_pattern": "orders"
        },
        {
          "layer": "service",
          "file_pattern": "order-service"
        },
        {
          "layer": "database",
          "file_pattern": "repository"
        }
      ]
    },
    
    {
      "id": "user_creation",
      "name": "User Registration Flow",
      "enabled": true,
      "description": "Monitor user registration with password hashing and validation",
      "routes": [
        "POST /api/users"
      ],
      "code_flow": [
        {
          "layer": "controller",
          "file_pattern": "users"
        },
        {
          "layer": "service",
          "file_pattern": "user-service"
        },
        {
          "layer": "database",
          "file_pattern": "repository"
        }
      ]
    },
    
    {
      "id": "product_management",
      "name": "Product Creation Flow",
      "enabled": true,
      "description": "Monitor product catalog operations",
      "routes": [
        "POST /api/products"
      ],
      "code_flow": [
        {
          "layer": "controller",
          "file_pattern": "products"
        },
        {
          "layer": "database",
          "file_pattern": "repository"
        }
      ]
    },
    
    {
      "id": "user_retrieval",
      "name": "User Data Retrieval",
      "enabled": false,
      "description": "Monitor user data access patterns (disabled - example of partial flow)",
      "routes": [
        "GET /api/users/:id",
        "GET /api/users"
      ],
      "code_flow": [
        {
          "layer": "controller",
          "file_pattern": "users"
        },
        {
          "layer": "service",
          "file_pattern": "user-service"
        },
        {
          "layer": "database",
          "file_pattern": "repository"
        }
      ]
    }
  ]
}